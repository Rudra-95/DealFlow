import { ChevronRight } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { MetricCard, PageHeader, StatusBadge, Table } from '../components/shared';

export function Fulfillment() { const navigate = useNavigate(); const rows = [['ORD-8841','Northstar Labs','Edge Gateway Pro','Main 8 · East 4','12','Split pending'],['ORD-8838','Veridian Health','Signal Router','Main 0 · East 4','6','Backorder'],['ORD-8832','Atlas Freight','Fleet Beacon','Main 10 · East 0','10','Pending']]; return <><PageHeader eyebrow="Operations / Fulfillment" title="Fulfillment queue" description="See inventory reality before it becomes a customer surprise." /><div className="metric-grid compact"><MetricCard label="Awaiting fulfillment" value="18" detail="Across 6 active orders" tone="blue" /><MetricCard label="Split pending" value="6" detail="Need allocation decisions" tone="amber" /><MetricCard label="Backordered units" value="24" detail="3 customer orders" tone="coral" /></div><Table><thead><tr><th>Order</th><th>Customer</th><th>Product</th><th>Warehouse availability</th><th>Units</th><th>Status</th><th /></tr></thead><tbody>{rows.map((row) => <tr key={row[0]} onClick={() => navigate(`/fulfillment/${row[0]}`)}>{row.slice(0,5).map((cell, index) => <td key={cell}>{index === 0 ? <strong>{cell}</strong> : cell}</td>)}<td><StatusBadge status={row[5]} /></td><td><ChevronRight size={17} /></td></tr>)}</tbody></Table></> }
